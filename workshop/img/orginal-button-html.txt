<button class="steadman-btn-primary">
  Get Started
</button>

<style>
  /* Import Space Grotesk if not already linked */
  @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;700&display=swap');

  :root {
    --steadman-navy: #1C2B42;
    --steadman-cream: #F7F5EC;
    --steadman-orange: #C05621; /* Burnt Orange */
    --steadman-shadow: #1C2B42; /* Using Navy for the shadow */
  }

  .steadman-btn-primary {
    /* Typography */
    font-family: 'Space Grotesk', sans-serif;
    font-weight: 700;
    font-size: 1rem;
    letter-spacing: -0.02em; /* Tighter tracking for modern look */
    
    /* Box Model */
    background-color: var(--steadman-orange);
    color: var(--steadman-cream);
    padding: 12px 24px;
    border: 2px solid var(--steadman-navy); /* Solid border gives the "ink" look */
    border-radius: 6px; /* Slight rounding, not a pill shape */
    cursor: pointer;
    
    /* Animation Setup */
    position: relative;
    transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    box-shadow: 0px 0px 0px 0px var(--steadman-shadow);
  }

  /* The "Tactile" Hover Effect */
  .steadman-btn-primary:hover {
    /* Move button Up and Left */
    transform: translate(-4px, -4px);
    
    /* Create a hard, solid shadow (no blur) where the button used to be */
    box-shadow: 4px 4px 0px 0px var(--steadman-shadow);
  }

  /* Active State (Clicking) */
  .steadman-btn-primary:active {
    transform: translate(0px, 0px);
    box-shadow: 0px 0px 0px 0px var(--steadman-shadow);
  }
</style>